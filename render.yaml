services:
  - type: web
    name: python-runner-ui
    runtime: python
    plan: starter
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
